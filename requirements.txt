anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
numpy>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Cloud monitoring (optional; only needed when deploying to Azure).
# The app runs fine without these — monitoring degrades gracefully.
azure-monitor-opentelemetry>=1.6.0
opentelemetry-instrumentation-fastapi>=0.48b0
